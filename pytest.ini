# pytest.ini
[pytest]
pythonpath = app/backend
testpaths = tests
markers =
    integration: marks tests as integration tests (need running API + real keys)
addopts = -v -m "not integration"

[flask]
app = app.backend.app:create_app
